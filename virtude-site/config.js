/* =====================================================================
   CONFIGURAÇÃO DO SITE — Igreja Virtude
   ---------------------------------------------------------------------
   Preencha os campos abaixo para LIGAR o modo automático.
   Enquanto estiverem vazios, o site usa o conteúdo fixo de dados.json
   (ou seja: nunca fica quebrado).

   - googleApiKey : chave da API do Google (ative YouTube Data API v3
                    e Google Calendar API no console.cloud.google.com).
   - calendarId   : ID da sua Google Agenda PÚBLICA
                    (ex.: "abc123@group.calendar.google.com").
   - youtubeChannelId : já preenchido com o canal @IgrejaVirtude.
   ===================================================================== */
window.VIRTUDE_CONFIG = {
  googleApiKey: "",
  calendarId: "",
  youtubeChannelId: "UChNaIs-7gpr4ZTaHYXpV8RQ"
};
